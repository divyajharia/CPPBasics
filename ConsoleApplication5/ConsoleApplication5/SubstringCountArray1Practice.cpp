#include "stdafx.h"
#include <vector>
#include <iostream>
#include <string>
#include <array>
#include <map>
using namespace std;

//int main()
//{
//    string a = "abbsnhnabsn";
//    map<pair<char, char>, int> lexico;
//    for (int i = 0; i < a.size()-1; i++)
//    {
//        lexico[make_pair(a[i], a[i + 1])]++;
//    }
//
//    // Traverse map to print sub-strings and their
//    // counts.
//    cout << "Distinct sub-strings with counts:\n";
//    for (auto it = lexico.begin(); it != lexico.end(); it++)
//        cout << it->first.first << it->first.second
//        << "-" << it->second << " ";
//}