#include "stdafx.h"
#include <vector>
#include <iostream>
#include <string>
#include<stdio.h>
#include <sstream>
using namespace std;

// returns the length of the given wide string(UNICODE) 
//  wcsnlen - determine the length of a fixed-size wide-character string

//int main()
//{
//
//    const wchar_t *  w = L"divya jharia";
//    int a = 100;
//      int ab =  wcsnlen(w, a);
//      cout << ab << "  "<< w << "  "<< a <<" " << sizeof(w)<<"  "<< *w << endl;
//
//      int abb = wcslen(w);
//      cout << abb << "  " << w << "  " << a << " " << sizeof(w) << "  " << *w << endl;
//}